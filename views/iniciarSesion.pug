<!DOCTYPE html>

html(lang="es")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Iniciar sesión en Uptask
            if error
                p.alerta.error= error
            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") E-mail:
                    input(type="text" name="email" placeholder="Aquí va tu email" value= email)
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Aquí va tu password" value= password)
                .campo.enviar
                    input.boton(type="submit" value="Iniciar sesion")
                .campo
                    a(href="/crear-cuenta") Crea una cuenta gratis
